// 菜单类型
export enum MenuEnum {
  CATALOGUE = 'M', // 目录
  MENU = 'C', // 菜单
  BUTTON = 'A', // 按钮
}

// 屏幕
export enum ScreenEnum {
  SM = 640,
  MD = 768,
  LG = 1024,
  XL = 1280,
  '2XL' = 1536,
}

//菜单主题类型
export enum ThemeEnum {
  LIGHT = 'light',
  DARK = 'dark',
}
